# Reporte Consolidado de Acciones de Stock

**Generado el:** 26/11/2025 a las 09:51:10

**Criterios del Análisis:**
- **Bajo Stock si:** Cantidad de paquetes ≤ `5`
- **Fuente Viable si:** Cantidad de paquetes ≥ `1`

---

## Producto ID: `GS-046`

**Inventario Actual:**
- Paquetes de `25`: **0** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 0). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-082`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-013`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-037`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-023`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **5** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-070`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GPP-018AG`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **7** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-066`

**Inventario Actual:**
- Paquetes de `25`: **6** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **19** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GPP-004AG`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **19** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-117`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **10** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-001`

**Inventario Actual:**
- Paquetes de `22`: **1** unidades
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **19** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-024`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `RA-002AG`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **7** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `COM-004`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **11** unidades
- Paquetes de `100`: **7** unidades
- Paquetes de `494`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-006`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-022`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-063AG`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **7** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GM-001`

**Inventario Actual:**
- Paquetes de `25`: **20** unidades
- Paquetes de `50`: **16** unidades
- Paquetes de `100`: **5** unidades
- Paquetes de `500`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `100` (quedan 5). **Solución:** Convertir 1 paquete de `500` para generar **5** nuevos paquetes.

---

## Producto ID: `GC-021`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-050`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **6** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GPV-013`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `CH-010C`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **6** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-005`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **6** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-041M`

**Inventario Actual:**
- Paquetes de `25`: **8** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **26** unidades
- Paquetes de `498`: **1** unidades
- Paquetes de `500`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-015`

**Inventario Actual:**
- Paquetes de `25`: **0** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 0). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-095`

**Inventario Actual:**
- Paquetes de `25`: **8** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **61** unidades
- Paquetes de `500`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-073`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **6** unidades
- Paquetes de `100`: **26** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GPV-010`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GPV-021`

**Inventario Actual:**
- Paquetes de `25`: **8** unidades
- Paquetes de `50`: **8** unidades
- Paquetes de `100`: **3** unidades
- Paquetes de `500`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `100` (quedan 3). **Solución:** Convertir 1 paquete de `500` para generar **5** nuevos paquetes.

---

## Producto ID: `GTB-001`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **1** unidades
- Paquetes de `500`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-023`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-035`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-004`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `COM-005`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-092`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **8** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-040`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **9** unidades
- Paquetes de `100`: **45** unidades
- Paquetes de `445`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `RP-002AG`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **38** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-089`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **8** unidades
- Paquetes de `100`: **25** unidades
- Paquetes de `500`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-046`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-007`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-006`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-022`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-029G`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-011`

**Inventario Actual:**
- Paquetes de `25`: **0** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `TR-1161`

**Inventario Actual:**
- Paquetes de `5`: **1** unidades
- Paquetes de `9`: **1** unidades
- Paquetes de `10`: **95** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `5` (quedan 1). **Solución:** Convertir 1 paquete de `10` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-024`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-042`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **12** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-009`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-049`

**Inventario Actual:**
- Paquetes de `25`: **0** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-021`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-034`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GPV-004`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **5** unidades
- Paquetes de `456`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-066AG`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **7** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-033AG`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **18** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-153`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-006`

**Inventario Actual:**
- Paquetes de `25`: **6** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **26** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `CH-011C`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-038`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-054`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-048`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-041`

**Inventario Actual:**
- Paquetes de `25`: **29** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **207** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `COM-007`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **7** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-012`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-062`

**Inventario Actual:**
- Paquetes de `25`: **0** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 0). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-048`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-019`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-045`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `CH-001L`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **13** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-136`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **10** unidades
- Paquetes de `100`: **14** unidades
- Paquetes de `500`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-087`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **7** unidades
- Paquetes de `100`: **17** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-003L`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **12** unidades
- Paquetes de `100`: **26** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-133`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-040`

**Inventario Actual:**
- Paquetes de `25`: **6** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-138`

**Inventario Actual:**
- Paquetes de `25`: **7** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **5** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-055`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **26** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `COM-003`

**Inventario Actual:**
- Paquetes de `25`: **6** unidades
- Paquetes de `50`: **12** unidades
- Paquetes de `100`: **5** unidades
- Paquetes de `451`: **1** unidades
- Paquetes de `500`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `100` (quedan 5). **Solución:** Convertir 1 paquete de `500` para generar **5** nuevos paquetes.

---

## Producto ID: `GC-044`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **9** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `2288`

**Inventario Actual:**
- Paquetes de `25`: **18** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **6** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-047`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-028`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GPV-011`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `TT-003N`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **30** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-014L`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-041L`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-037`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-023`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GV-008`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **10** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-065`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **7** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-001AG`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **38** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-032`

**Inventario Actual:**
- Paquetes de `25`: **13** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **19** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GPP-001`

**Inventario Actual:**
- Paquetes de `25`: **6** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **7** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-009L`

**Inventario Actual:**
- Paquetes de `25`: **7** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **15** unidades
- Paquetes de `1000`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-031`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GPP-011`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-019`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-015`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-003`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-042`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **5** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-011`

**Inventario Actual:**
- Paquetes de `25`: **7** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **6** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-155`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **5** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-015`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **7** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `COM-002`

**Inventario Actual:**
- Paquetes de `25`: **13** unidades
- Paquetes de `50`: **11** unidades
- Paquetes de `100`: **4** unidades
- Paquetes de `500`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `100` (quedan 4). **Solución:** Convertir 1 paquete de `500` para generar **5** nuevos paquetes.

---

## Producto ID: `GD-017`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `TT-001`

**Inventario Actual:**
- Paquetes de `25`: **6** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **8** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-156`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **7** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-013`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-041N`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **24** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GPP-014`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-093B`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **9** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-058`

**Inventario Actual:**
- Paquetes de `25`: **10** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **39** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-021`

**Inventario Actual:**
- Paquetes de `25`: **6** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-058`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **10** unidades
- Paquetes de `100`: **5** unidades
- Paquetes de `497`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GPV-008AG`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **6** unidades
- Paquetes de `100`: **12** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-026`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-065`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **59** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-045`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-025`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **14** unidades
- Paquetes de `100`: **48** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GPV-028`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **8** unidades
- Paquetes de `100`: **10** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-020`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-035`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-041C`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-137`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-068`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **10** unidades
- Paquetes de `500`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-129`

**Inventario Actual:**
- Paquetes de `25`: **7** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **11** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-015`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-001AG`

**Inventario Actual:**
- Paquetes de `25`: **0** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-014`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-044`

**Inventario Actual:**
- Paquetes de `25`: **0** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 0). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-013`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **0** unidades
- Paquetes de `100`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **4** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 0). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-012`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-003AG`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-001`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-019`

**Inventario Actual:**
- Paquetes de `25`: **9** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **6** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-011`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-063AG`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **13** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GPP-010`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-022B`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **21** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `CH-003C`

**Inventario Actual:**
- Paquetes de `25`: **46** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **2** unidades
- Paquetes de `500`: **14** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `100` (quedan 2). **Solución:** Convertir 1 paquete de `500` para generar **5** nuevos paquetes.

---

## Producto ID: `GS-063`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-057`

**Inventario Actual:**
- Paquetes de `22`: **1** unidades
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **3** unidades
- Paquetes de `474`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-041`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **8** unidades
- Paquetes de `100`: **11** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GPP-006`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **6** unidades
- Paquetes de `100`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `COM-008`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **7** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-013`

**Inventario Actual:**
- Paquetes de `25`: **2** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **5** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 2). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `AC-002`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **4** unidades
- Paquetes de `100`: **7** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 4). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `CH-011L`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **3** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GC-018`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **6** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-051`

**Inventario Actual:**
- Paquetes de `25`: **0** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **1** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 0). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 1). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `CH-007C`

**Inventario Actual:**
- Paquetes de `25`: **0** unidades
- Paquetes de `50`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 0). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-017G`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **7** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GP-005`

**Inventario Actual:**
- Paquetes de `25`: **1** unidades
- Paquetes de `50`: **1** unidades
- Paquetes de `100`: **0** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 1). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-009`

**Inventario Actual:**
- Paquetes de `25`: **8** unidades
- Paquetes de `50`: **3** unidades
- Paquetes de `100`: **21** unidades
- Paquetes de `500`: **4** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `50` (quedan 3). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-059`

**Inventario Actual:**
- Paquetes de `25`: **4** unidades
- Paquetes de `50`: **5** unidades
- Paquetes de `100`: **24** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 4). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 5). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

## Producto ID: `GS-147`

**Inventario Actual:**
- Paquetes de `25`: **5** unidades
- Paquetes de `50`: **9** unidades
- Paquetes de `100`: **7** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 5). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.

---

## Producto ID: `GD-055`

**Inventario Actual:**
- Paquetes de `25`: **3** unidades
- Paquetes de `50`: **2** unidades
- Paquetes de `100`: **2** unidades

**Acciones Recomendadas:**

> **Problema:** Stock bajo en paquetes de `25` (quedan 3). **Solución:** Convertir 1 paquete de `50` para generar **2** nuevos paquetes.
> **Problema:** Stock bajo en paquetes de `50` (quedan 2). **Solución:** Convertir 1 paquete de `100` para generar **2** nuevos paquetes.

---

